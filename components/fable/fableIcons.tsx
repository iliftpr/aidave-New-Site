// Curated lucide icon map for the /fable page, keyed by the `icon` strings in
// lib/fable-content.ts. Keeping this small avoids shipping the whole icon set.
import {
  Target,
  Search,
  Swords,
  ShieldCheck,
  FileText,
  Globe,
  BookOpen,
  Users,
  MousePointerClick,
  ScanSearch,
  Workflow,
  type LucideIcon,
} from 'lucide-react'

export const FABLE_ICONS: Record<string, LucideIcon> = {
  Target,
  Search,
  Swords,
  ShieldCheck,
  FileText,
  Globe,
  BookOpen,
  Users,
  MousePointerClick,
  ScanSearch,
  Workflow,
}
